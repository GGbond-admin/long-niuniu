# 至尊牛牛 — 公式与数值配置总表

> 全部游戏公式的唯一权威汇总，供开发实现与运营核对。  
> 标注【配置】= 后台可修改项；标注【固定】= 规则固定逻辑。  
> 版本：v1.0（2026-08-06，对应 PRD v1.7）

---

## 1. 点数与牌型

### 1.1 点数【固定】

```
点数 = (红包金额的所有数字之和) 的个位数
三位数字之和刚好 = 10 → 不按点数计算，牌型判为「牛牛」（见 1.2）
个位数为 0 且非「牛牛」（如相加 = 20）→ 记 10 点（最大点数）
```

例：`3.42` → 3+4+2 = 9 → **9 点**；`9.83` → 9+8+3 = 20 → **10 点**；`2.80` → 2+8+0 = 10 → **牌型「牛牛」**

### 1.2 牌型判定（由高到低）【倍数配置】

红包金额固定两位小数，取三位数字 `[a, b, c]`（a=整数位个位，b、c=小数两位）：

| 优先级 | 牌型 | 判定条件 | 默认倍数 | 示例 |
|---|---|---|---|---|
| 0 | 免死 | 金额固定 = 0.01 | 判和（不参与倍数赔付） | 0.01 |
| 1 | 豹子 | a=b=c | 17 | 1.11 / 7.77 |
| 2 | 满牛 | 后两位为 00（b=c=0） | 15 | 5.00 / 88.00 |
| 3 | 反顺（倒顺） | 连续递减 b=a−1 且 c=b−1；`0.98` 为钦定特例 | 14 | 9.87 / 2.10 / 0.98 |
| 4 | 顺子 | 连续递增 b=a+1 且 c=b+1（0 可作起点） | 13 | 0.12 / 7.89 |
| 5 | 对子 | b=c 且非零 | 12 | 1.22 / 7.55 |
| 6 | 金牛 | `0.X0` 形式（a=0、c=0、b=1–9） | 11 | 0.10 / 0.50 / 0.90 |
| 7 | 牛牛 | a+b+c 刚好 = 10（兜底于以上牌型之后） | 10 | 2.35 / 1.36 / 5.50 |
| 8 | 普通 | 其余 | 按点数 1–10 各自倍数【配置】 | 3.42 / 9.83 |

**优先级说明**：同时符合多个牌型时取表中更高的牌型。因此 `0.55`（和 = 10 且末两位相同）判**对子**而非牛牛，`1.36`（递增但不连续）判**牛牛**，`0.05`（非 `0.X0`）判**普通 5 点**。

**免死（0.01）规则**：抢到 RM0.01 的一方本对直接判和——退回本金、不赔不赚、不抽水、永不自爆；庄家抢到 0.01 则本局所有对子判和。

---

## 2. 胜负比较【固定，已确认】

```
第零步：任一方为免死（0.01）→ 本对直接判和，不再比牌
第一步：比牌型等级（豹子 > 满牛 > 反顺 > 顺子 > 对子 > 金牛 > 牛牛 > 普通）
  等级不同 → 高等级胜
第二步：等级相同（含同点数）→ 比红包金额，金额大者胜
  例：2.80 赢 1.09（同为牛牛，金额大者胜）
第三步：金额完全相同 → 平局，该对不结算，下注原路退回
```

### 2.1 自爆【阈值配置，规则已确认】

```
点数 ≤ 3（1/2/3 点，普通牌型）→ 自爆，直接判输（💥 标记）
庄家 3 点及以下 → 同样自爆，输给所有未自爆闲家
庄闲双方都自爆 → 庄家赢
特殊牌型（金牛、牛牛等）→ 默认不判自爆（豁免开关【配置】）
  注：`0.02`/`0.03` 不再属于金牛，按普通 2/3 点判自爆
```

---

## 3. 下注 / 梭哈动态范围【已确认公式，系数配置】

```
下注下限 = 2（固定，可配）
房间下注上限 = 庄钱 × 0.5% × 人数系数
梭哈下限 = 20（固定，可配）
梭哈上限 = 玩家当前可承担余额（精确到分，可全押；不受庄钱 / 人数系数限制）

本局最高赔付倍数 M = max(特殊牌型倍数, 普通 1–10 点倍数)
默认 M = 17；若后台配置更高倍数，则按实际最高值

普通下注：本笔赔付倍数 = M
          赔付能力上限 = floor(当前可承担余额 ÷ M ÷ 1 RM) × 1 RM
          最终接受 = min(用户输入, 房间下注上限, 赔付能力上限)
梭哈：    本笔赔付倍数 = 1（固定 1:1，与牌型无关）
          最终接受 = min(用户输入, 当前可承担余额)
最大赔付预留金 = 最终接受下注 × 本笔赔付倍数
```

| 庄钱 | 普通下注范围 | 梭哈 |
|---|---|---|
| 5,000 | 2 ~ 25 | 最低 20，上限为各自余额 |
| 8,540 | 2 ~ 42.7 | 最低 20，上限为各自余额 |
| 10,000 | 2 ~ 50 | 最低 20，上限为各自余额 |

**人数系数**：人数少 → 系数调高（让单人可下更多），人数多 → 系数回落，防止总注超出庄池赔付能力（防「喝水」）。具体分档表【配置，待运营定，Q17】，示例结构：

| 参与人数 | 系数示例 |
|---|---|
| < 10 人 | ×2.0 |
| 10 ~ 20 人 | ×1.5 |
| > 20 人 | ×1.0 |

余额风控示例（普通下注，默认 M=17）：

| 当前可承担余额 | 用户输入 | 赔付能力上限 | 实际接受 | 冻结预留金 |
|---|---|---|---|---|
| 200 | 50 | 11 | 11 | 187 |
| 500 | 100 | 29 | 29 | 493 |
| 1,000 | 100 | 58 | 58 | 986 |
| 1,700 | 100 | 100 | 100 | 1,700 |

余额风控示例（梭哈，倍数固定 1，不受房间庄钱上限限制）：

| 当前可承担余额 | 用户输入 | 赔付能力上限 | 实际接受 | 冻结预留金 |
|---|---|---|---|---|
| 200 | 500 | 200 | 200 | 200 |
| 123.45 | 123.45 | 123.45 | 123.45 | 123.45 |
| 800 | 600 | 800 | 600 | 600 |

- 普通下注输入高于房间上限或余额能力：自动降额，不整笔拒绝；必须私下明确显示输入、余额、最高可下注及实际接受。
- 梭哈输入高于余额：自动降到余额本身；余额低于梭哈下限 20 才拒绝。
- 庄钱再小也不关闭梭哈。庄钱不够赔时，按牌型 / 点数 / 红包金额 / 下注时间统一排队，排后者喝水。
- 修改下注时，将原预留金计回可承担余额后重新计算，只补冻/解冻差额。
- 指令：普通下注发送金额数字；梭哈发送 `sh金额`；发送 `0` 撤回并释放完整预留金（封盘前）。

---

## 4. 抽水【已确认：只抽赢方，玩家赢 3% / 庄家赢 5%，与费用并存】

```
闲家赢：抽水 = 闲家该笔盈利 × 3%（playerRakeRatio）
        闲家实得 = 本笔赔付倍数 × 下注 − 抽水（梭哈倍数固定 1）
庄家赢：抽水 = 庄家该笔实收（本笔赔付倍数 × 下注，从闲家预留金支付）× 5%（bankerRakeRatio）
        庄家实得 = 实收 − 抽水
平局：不抽水
输方：不额外抽水
```

- 玩家赢抽水比例默认 3%、庄家赢抽水比例默认 5%，均【配置】（游戏配置 → 费用与抽水）。
- 历史局兼容：拆分前的配置快照只有单一 `rakeRatio`，进行中的旧局两侧均按旧值抽取。
- 全部抽水实收入账 `PLATFORM_RAKE`，作为「利润池」的毛利润来源（见《07-利润池与称桶分配》）。

---

## 5. 庄家费用【已确认公式】

| 费用 | 公式 | 示例 | 配置项 |
|---|---|---|---|
| 上庄费 | 庄钱 × 1% | 5000 → 50；7776 → 77.76 | 比例【配置】 |
| 服务费 | 每场固定金额 | 38.00 / 场 | 金额【配置】 |
| 代包费 | = 本局 TNG 红包总额 = 参与人数 × 人均单价 | 30 人 × 1.04 = 31.20 | 人均单价【配置】 |

- 三项均由庄家承担，与抽水**并存**。
- 代包费覆盖平台代发红包成本（与红包备付账勾稽）。

### 5.1 红包总额【已确认】

```
红包总额 = 参与人数（庄家 + 已下注闲家）× 人均单价
```

示例：30 人 × 1.04 = **31.2**（人均单价【配置】）

---

## 6. 单局结算总公式

### 6.1 每对庄闲结算【固定流程】

```
对每个已下注闲家 i：
  若 任一方为免死（0.01）→ 本对判和
  否则若 闲家_i 自爆（点数≤3）→ 闲输
  否则若 庄家自爆 → 闲赢
  否则按第 2 节比较规则定胜负

  本笔赔付倍数 R_i = 梭哈单 ? 1 : 赢方牌型倍数   ← 梭哈固定 1:1，牌型倍数只用于定胜负与排序

  闲赢_i：应付 = R_i × 下注_i
          实付 = min(应付, 庄池剩余)      ← 按 6.1a 赔付顺序依次扣减庄池
          抽水 = 实付 × 3%（玩家赢抽水）
          闲家_i 实得 = 实付 − 抽水
  闲输_i：应付 = R_i × 下注_i             ← 普通按庄家牌型倍数（金牛 11 倍…），梭哈按 1 倍
          实收 = min(应付, 闲家_i 最大赔付预留金)
          抽水 = 实收 × 5%（庄家赢抽水）
          庄家实得 = 实收 − 抽水
          未使用预留金 = 最大赔付预留金 − 实收 → 解冻退回
  闲赢/平局_i：闲家最大赔付预留金全额解冻，不抽输方
```

梭哈例（默认倍数表）：梭哈 RM100 抢到豹子（牌型 17 倍）→ 赢只拿 **100**（抽水 3 → 实得 97）；同样 RM100 梭哈抢到普通 6 点、庄家豹子 → 只赔 **100**（庄家抽水 5 → 实得 95）。免死（0.01）与自爆（≤3 点）判定对梭哈同样适用，只是金额按 1 倍计。

例（金牛 11 倍）：闲家下注 3，下注时按最高 17 倍预留 51；庄家抢到金牛时实扣 **33**，剩余 **18** 解冻退回。正常新下注不会发生闲家赔付不足；`shortfall` 仅保留用于部署前旧单或异常数据防御。

### 6.1a 庄钱不足时的赔付顺序【固定】

```
庄钱 = 庄家本局赔付上限，赔完即止（与闲家自身余额无关）
闲家赢的队列排序（普通与梭哈全部赢家一起排，不区分下注模式，非按下注顺序）：
  ① 牌型等级 高 → 低（梭哈按自己抢到的牌型排，不按 1:1 降级；按等级不按后台倍数，改倍数不改赔付先后）
  ② 同牌型 → 点数 大 → 小
  ③ 同牌型同点数 → 红包金额 大 → 小
  ④ 全部相同 → 下注时间 早 → 晚
依序赔付：
  实付_i = min(应付_i, 庄池剩余)；庄池剩余 -= 实付_i
  实付 < 应付 → 差额记 shortfall（免赔）
  庄池剩余 = 0 后的赢家 → 实付 0，全额记 shortfall，成绩单标「喝水」
  喝水者的下注预留金全额解冻退回，不倒扣
```

例（庄钱 100,000）：A 17 倍 8 点应赔 51,000 → 全额；B 11 倍 9 点应赔 33,000 → 全额（剩 16,000）；C 11 倍 7 点应赔 22,000 → 只得 16,000（免赔 6,000）；D 普通 9 点应赔 5,000 → 喝水 0。

### 6.2 庄家本局净结果【固定】

```
庄家盈利（毛） = Σ(实收闲家赔付净额) − Σ(赔付给闲家的金额)   ← 已扣抽水
庄家实际盈利 = 庄家盈利 − 上庄费 − 服务费 − 代包费
局末：庄池剩余 + 净结果相关结转 → 退回庄家可用余额
```

成绩单庄家区块口径（须逐项对得上账）：

| 项目 | 取值 |
|---|---|
| 庄家盈利 | 毛盈利（已扣抽水） |
| 上庄费 / 服务费 / 代包费 | 三费逐项 |
| 庄家实际盈利 | 盈利 − 三费 |
| 上庄积分 | 本局结算前庄家总积分（余额口径） |
| 盈利 | = 庄家实际盈利 |
| 庄总积分 | = 上庄积分 + 盈利（即结算后总积分） |

另单列「累计做庄盈亏」为跨局统计，不与本局积分混用。

---

## 7. 推广返水【已确认】

```
本人佣金（每日） = 自身有效下注 × 0.7%
                 + 直属下线有效下注 × 0.5%
                 + 二级下线有效下注 × 0.3%
```

- 比例三项均【配置】；最多向上两级（A→B→C→D 时 A 拿不到 D）。
- **有效下注口径**：
  - 闲家 = 参与结算的下注（输赢均计）
  - 庄家 = 本局对赌结算的闲家下注总额（**不按庄钱**，输赢均计）
  - 不计：撤回、取消局、弃权、竞标庄钱本身；平局默认不计【开关】
- 每日自动结算、实时发放入余额。

示例：自身 1000 + 直属 2000 + 二级 500 → 7 + 10 + 1.5 = **RM 18.50**

---

## 8. 每日奖励【金额/份数/门槛全配置】

### 8.1 棋牌奖励（门槛：单局下注 ≥ 5 或梭哈 ≥ 50）

| 成就 | 条件 | 默认金额 | 默认份数/日 |
|---|---|---|---|
| 怪牌奖励 | 豹子+顺子+反顺+满牛 各 1 次 | RM 388.88 | 3 |
| 豹子王 | 豹子 ×3 | RM 288.88 | 3 |
| 满牛王 | 满牛 ×3 | RM 288.88 | 3 |
| 顺子王 | 顺子 ×3 | RM 188.88 | 3 |
| 反顺王 | 反顺 ×3 | RM 188.88 | 3 |

### 8.2 庄家奖励

做庄（打桩）次数阶梯（当日）：

| 次数 | 奖励 |
|---|---|
| 18 | RM 288.00 |
| 28 | RM 388.00 |
| 58 | RM 888.00 |
| 88 | RM 1,388.00 |
| 108 | RM 1,888.00 |

庄家秒杀奖励：做庄抢到 **RM 0.01 × 3 次** → **RM 588.00**（限量，示例 1 份/日）。

### 8.3 特别奖励

后台自由配置并推送，无固定公式。

通用：每日 0 点重置；份数领完即止；得奖名单页面展示；达成自动入账 + Bot 播报。

---

## 9. 排行榜【口径/周期/TopN 配置】

| 榜单 | 排名依据 | 数据源 |
|---|---|---|
| 积分排行榜 | 积分/累计输赢【口径配置】 | wallet/结算流水 |
| 棋牌排行榜 | 特殊牌型达成数 | claims 牌型统计 |
| 打桩排行榜 | 做庄次数 | banker_stats.rounds_as_banker |

---

## 10. 时间参数【全配置】

| 参数 | 默认值 |
|---|---|
| 竞标庄钱倒计时 | 30 秒（结束后另有 3/2/1 收官确认） |
| 下注倒计时 | 50 秒 |
| 抢包倒计时（红包过期） | 30 秒 |
| 返水结算 | 每日（实时发放） |
| 奖励/进度重置 | 每日 0 点（GMT+8） |

---

## 11. 配置项清单（后台「游戏配置」应含）

1. 牌型倍数 ×7（豹子/满牛/反顺/顺子/对子/金牛/牛牛 + 普通 1–10 点位表）
2. 抽水比例（玩家赢默认 3% / 庄家赢默认 5%，只抽赢方）
3. 自爆阈值（默认 ≤3 点）与特殊牌型豁免开关  
4. 普通下注范围：下限、庄钱比例（0.5%）、人数系数表；梭哈只配最低额，上限为玩家余额
5. 上庄费比例（1%）、服务费（38/场）、红包人均单价（≈1.04）  
6. 倒计时：竞标 / 下注（50s）/ 抢包（30s）  
7. 返水比例（0.7 / 0.5 / 0.3）与平局计入开关  
8. 每日奖励：各成就条件、门槛（下注5/梭哈50）、金额、每日份数  
9. 庄家奖励阶梯与秒杀参数  
10. 排行榜口径、周期、Top N  
11. 续庄限制（每桌 1 次）、最低开局人数  
12. 群文案模板与置顶通告  

---

## 12. 待补参数（不阻塞开发，先做成配置）

| 项 | 说明 |
|---|---|
| 人数系数分档表 | Q17：各人数区间的范围上调倍数 |
| 竞标倒计时秒数 | 运营定 |
| 普通牌型 1–10 点倍数表初值 | 运营定 |
| 排行榜口径与周期默认值 | Q18 |
